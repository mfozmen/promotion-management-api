/**
 * How many validation details cross the boundary. One constant, because the
 * validator bounds the work of building them and the envelope bounds what is
 * returned, and two copies would have to be changed together by hand.
 */
export const MAX_DETAILS = 20;
