/**
 * Bounds with two enforcement sites that must agree: the validator builds
 * details and the envelope returns them; the envelope returns a message and
 * the logger records one. A bound enforced in one place belongs beside it.
 */
export const MAX_DETAILS = 20;
export const MAX_MESSAGE = 200;
